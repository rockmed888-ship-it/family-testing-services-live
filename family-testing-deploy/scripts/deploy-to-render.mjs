#!/usr/bin/env node
console.error("Direct Render API creation is disabled for this release.");
console.error("Use Blueprint sync with render.yaml so the paid persistent disk and required secrets are created together.");
console.error("See RENDER_SETUP.md.");
process.exit(1);
